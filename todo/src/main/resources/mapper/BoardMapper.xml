<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.domain.todo.board.mapper.BoardMapper">

    <select id="getTodoList" parameterType="map" resultType="map">
        SELECT
            todo_id AS TODO_ID,
            content AS CONTENT,
            DATE_FORMAT(regist_date, '%Y-%m-%d') AS registDate,
            CASE
                WHEN completed_yn = 'Y' THEN 1
                when completed_yn = 'n' then 0
                ELSE 0
            END AS STATUS
        FROM board
        <where>
            delete_flag_yn = 'N'
            <if test="search != null and search != ''">
                AND content LIKE CONCAT('%', #{search}, '%')
            </if>
            <if test="status != null">
                AND completed_yn = CASE WHEN #{status} = 1 THEN 'Y' ELSE 'N' END
            </if>
        </where>
        ORDER BY
            CASE WHEN completed_yn = 'N' THEN 0 ELSE 1 END,
            regist_date DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="getTodoListCnt" parameterType="map" resultType="int">
        SELECT COUNT(*)
        FROM board
        <where>
            delete_flag_yn = 'N'
            <if test="search != null and search != ''">
                AND content LIKE CONCAT('%', #{search}, '%')
            </if>
            <if test="status != null">
                AND completed_yn = CASE WHEN #{status} = 1 THEN 'Y' ELSE 'N' END
            </if>
        </where>
    </select>

    <select id="getTodoDetail" parameterType="int" resultType="map">
        SELECT
            todo_id AS TODO_ID,
            content AS CONTENT,
            DATE_FORMAT(regist_date, '%Y-%m-%d') AS registDate,
            CASE
                WHEN completed_yn = 'Y' THEN 1
                WHEN completed_yn = 'N' THEN 0
                ELSE 0
            END AS STATUS
        FROM board
        WHERE todo_id = #{todoId}
            AND delete_flag_yn = 'N'
    </select>

    <insert id="addTodo" parameterType="map" useGeneratedKeys="true" keyProperty="todoId">
        INSERT INTO board (
            content,
            completed_yn
        )
        VALUES (
            #{content},
            CASE
                WHEN #{status} = 1 THEN 'Y'
                WHEN #{status} = 0 THEN 'N'
                ELSE 'N'
            END
        )
    </insert>

    <update id="updateTodo" parameterType="map">
        UPDATE board
        SET
            content = #{content},
            completed_yn = CASE
                WHEN #{status} = 1 THEN 'Y'
                ELSE 'N'
            END
        WHERE todo_id = #{todoId}
    </update>

    <update id="updateTodoStatus" parameterType="map">
        UPDATE board
        SET
            completed_yn = CASE
                WHEN #{status} = 1 THEN 'Y'
                ELSE 'N'
            END
        WHERE todo_id = #{todoId}
    </update>

    <update id="deleteTodo" parameterType="int">
        UPDATE board
        SET
            delete_flag_yn = 'Y'
        WHERE todo_id = #{todoId}
            AND delete_flag_yn = 'N'
    </update>

</mapper>
